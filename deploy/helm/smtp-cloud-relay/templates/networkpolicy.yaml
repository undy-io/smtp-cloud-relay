{{- if .Values.networkPolicy.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "smtp-cloud-relay.fullname" . }}
  labels:
    {{- include "smtp-cloud-relay.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "smtp-cloud-relay.selectorLabels" . | nindent 6 }}
  policyTypes:
    - Ingress
    - Egress
  ingress:
    {{- if .Values.networkPolicy.ingressCIDRs }}
    - from:
        {{- range .Values.networkPolicy.ingressCIDRs }}
        - ipBlock:
            cidr: {{ . | quote }}
        {{- end }}
      ports:
        - protocol: TCP
          port: {{ .Values.smtp.port }}
        {{- if .Values.smtp.smtps.enabled }}
        - protocol: TCP
          port: {{ .Values.smtp.smtps.port }}
        {{- end }}
        - protocol: TCP
          port: {{ .Values.http.port }}
    {{- else }}
    []
    {{- end }}
  egress:
    {{- if .Values.networkPolicy.allowDNS }}
    - ports:
        - protocol: UDP
          port: 53
        - protocol: TCP
          port: 53
    {{- end }}
    {{- if .Values.networkPolicy.egressCIDRs }}
    - to:
        {{- range .Values.networkPolicy.egressCIDRs }}
        - ipBlock:
            cidr: {{ . | quote }}
        {{- end }}
      ports:
        {{- range .Values.networkPolicy.egressTCPPorts }}
        - protocol: TCP
          port: {{ . }}
        {{- end }}
    {{- else }}
    []
    {{- end }}
{{- end }}
