name: CI

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  validate:
    # Validation runs on GitHub-hosted runners; image smoke builds are not expected in the devcontainer.
    uses: ./.github/workflows/validate-reusable.yml
